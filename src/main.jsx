import React from 'react'
import ReactDOM from 'react-dom/client'
import HeatmapManager from './Index'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HeatmapManager />
  </React.StrictMode>,
)